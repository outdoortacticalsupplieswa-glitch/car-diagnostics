export const metadata = {
  title: "Precision Auto Diagnostic",
  description: "Advanced Vehicle Intelligence",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}
